#  AlgoStudio 算法工坊 
## 更新时间 
2026-08-10 15:19:59 
# 更新日志

## v1.0.3

* 解决切换操作系统的时候,加速器本来是-1,切换之后导致加速器改变的bug
* 不在判断Release版本,更新的时候都可以更新所有架构的版本
* 完成远程服务器工具的功能,包括安装显卡驱动,安装Docker,导入Docker镜像和下载镜像,安装加密狗驱动和重启加密狗服务
* SSHFtpManager支持连接ftp服务器和设置ftp服务的参数
* 支持时钟同步功能,包括设置Linux和Windows的时钟同步服务器
* 新增本地服务器环境安装界面,支持本地安装openssh-server
* 修复切换操作系统的时候,加速器类型消失的bug
* 升级jade_tools版本为2.5.2
* 修复Layout和Painter的警告和QNetworkAccessManager在子线程中运行的问题
* Debug模式下,日志级别为最低为:Trace
* 解决有旧通道,新建通道时,服务器的ip地址无法恢复的bug
* 解决新建通道的内存泄漏的问题
* 华为小站设备同步之前需要确保时区设置正确
* 日志中点击ImageView和VideoViewer窗口,可以激活窗口置顶窗口
* 测试箱号支持通道刷新,并且修复了没有通道的产生的bug
* 修复测试箱号识别服务的布局警告
* 修复第一次启动的时候,无法显示更新日志和设置代理的问题
* 更新临时文件目录
* Window窗口也是用一样的背景图片支持日夜切换
* 编译的时候生成更新文件,获取更新内容
* 解决新增相机直接预览崩溃的bug
* 相机添加辅助线,方便调试
* 相机区分箱号相机还是验残相机,主要就是顶部相机架设的位置不一样
* 获取屏幕的分辨率,并根据分辨率调整窗口大小
* 日志更新Dock栏,支持自动换行
* 解决更新后无法同步下发的bug
* 获取通道都是根据排序之后的通道
* 网络超时时间默认是30秒
* 只有创建通道保存的时候才会检查同一台服务器上的通道数是否超过最大通道数
* 支持远程SSH在Ubuntu26的版本
* 日志下载时,不需要校验本地文件是否存在
* 相机支持使用硬件编解码两个参数,默认是硬件编解码
* 停止通道需要弹窗确认
* 更新版本时候,下载之前查看本地的md5与在线版本比较,如果一致无需下载
* 解决无需下载的时候,加载框不消失的问题
* 测试添加获取算法服务health的接口
* 优化日志显示的性能,特别是历史日志大的时候,提高加载速度,并减少卡顿现象
* JComboBox 优化,在弹出之后,把当前项滚到居中位置
* 远程服务器在线apt-get update之前需要判断有无外网
* 添加工具合集卡片,提供下载链接地址
* 修复历史日志显示日志不全的问题
* 重新调整CUDA12对应的显卡驱动版本
* 添加EIO测试工具
* 箱号识别服务其他参数支持是否返回两个相似的箱号,相机参数支持是否开启检测集装箱门
* 关闭EIO工具窗口,需要强制关闭EIO连接
* 本地视频直接下载,不需要录制,网络视频无法直接下载,只能录制
* 去除设置中的下载缓存清理,由关于中的删除所有算法包进行清理,删除后同时需要更新版本
* 添加网络测试工具,支持测试服务器和所有的ip设备
* 支持本地机器测试网络连接,也支持远程服务器测试网络连接
* 箱号识别服务新增新增忽略集装箱检测高度比的参数
* 优化箱号是识别服务的参数配置
* 支持箱号识别服务Restful接口版本的设置
* 默认的压缩等级是6,之前是5,可以控制在单张图片在200KB以下
* 修改NetWork日志缓存的目录为network目录
* 支持V2接口测试
* 优化启动通道时的逻辑，如果远程文件不存在也需要上传文件
* 调整EIO Tools支持第一次连接返回失败,也支持中途连接失败
* EIO连接异常,自动关闭连接,并记录日志
* 箱号识别服务新增参数:仅适用于集装箱场站,表示该场站只识别集装箱车辆,如果识别结果是空车,返回异常

---